services:
  - type: web
    name: vehicle-number-plate-detection
    env: python
    region: Oregon
    buildCommand: export POETRY_VERSION= && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT

